@model IEnumerable<SocialNoteMark.Models.Answer>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.QuestionID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AnswererName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AnswerContent)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AnswerTime)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.QuestionID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AnswererName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AnswerContent)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AnswerTime)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AnswerID }) |
            @Html.ActionLink("Details", "Details", new { id=item.AnswerID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AnswerID })
        </td>
    </tr>
}
</table>
